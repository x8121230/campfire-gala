import PaperForestGame from './PaperForestGame.js';

// Preserve the old scene key for hub/handbook/map links, using the storybook app.
export default class PhantomRealmGame extends PaperForestGame {
 constructor(){super('PhantomRealmGame');}
}
